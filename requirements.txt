# =============================================================================
# Planetary Rover Navigation Simulator — Python dependencies
# =============================================================================
# Versions are pinned to the latest stable releases that are mutually
# compatible as of mid-2025.  Pin to patch level for reproducibility.
#
# Runtime (server)
# ────────────────
fastapi==0.115.6          # ASGI web framework — routes, dependency injection
uvicorn[standard]==0.32.1 # ASGI server with uvloop + httptools for performance
pydantic==2.10.3          # Data validation (FastAPI v0.115 requires Pydantic v2)

# Runtime (baseline client)
# ─────────────────────────
requests==2.32.3          # HTTP client used by baseline.py to call the server

# Runtime (inference.py — LLM-driven agent)
# ──────────────────────────────────────────
openai==1.57.4            # AsyncOpenAI client; supports any OpenAI-compatible endpoint
aiohttp==3.11.10          # Async HTTP client for non-blocking env server calls

# No further runtime dependencies.
# The simulator uses only Python stdlib: math, random, uuid, dataclasses,
# enum, typing — nothing else needs installing.

# Runtime (train.py — GRPO training pipeline)
# ────────────────────────────────────────────
torch>=2.1.0                  # PyTorch — GPU tensor computation
unsloth                       # 4-bit NF4 quantisation + fast LoRA
trl>=0.12.0                   # GRPOTrainer — group relative policy optimisation
datasets>=3.0.0               # Hugging Face Datasets — training data loader
peft>=0.13.0                  # Parameter-Efficient Fine-Tuning (LoRA adapters)
accelerate>=1.0.0             # Hugging Face Accelerate — distributed training utils
